# Git Commit & Push Safety Rules
- **NEVER** run `git commit`, `git stage/add`, or `git push` commands automatically.
- **ALWAYS** present all code modifications and diffs to the user for review first.
- Wait for explicit user confirmation (e.g., "Looks good, commit this" or "Go ahead and push") before executing any git operations that modify repository history.
